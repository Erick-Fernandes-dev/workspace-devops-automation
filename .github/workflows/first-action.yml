name: First Workflow

# Serve para disparar o workflow manualmente
on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World!"
      - name: Print goodbye
        run: echo "Done - bye"
      - name: Multiple commands
        run: |
          echo "First command"
          echo "Second command"
          echo "Third command"